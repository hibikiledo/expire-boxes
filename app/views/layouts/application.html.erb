<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <%= csrf_meta_tag %>
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <link rel="stylesheet" href="http://fonts.googleapis.com/css?family=Roboto:300,400,500,700" type="text/css">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <title>Expired Box : Dashboard</title>
  </head>
  <body>
    <!-- top nav -->
    <div class="navbar">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-warning-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">Expired Box</a>
        </div>
        <div class="navbar-collapse collapse navbar-warning-collapse">
          <ul class="nav navbar-nav">
            <li class="<%= 'active' if params[:controller] == 'dashboard/dashboard' && params[:action] == 'home' %>">
              <a href="/">Dashboard</a></li>
              <li class="<%= 'active' if params[:controller] == 'dashboard/boxes' && params[:action] == 'index' %>">
                <%= link_to "Boxes", dashboard_boxes_path %>
              </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
              <% if not user_signed_in? %>
              <li class="<%= 'active' if params[:controller] == 'devise/sessions' && params[:action] == 'new' %>">
                <%= link_to "Log in", new_user_session_path %>
              </li>
              <li class="<%= 'active' if params[:controller] == 'devise/registrations' && params[:action] == 'new' %>">
              <%= link_to "Sign up", new_user_registration_path %></li>
              <% else %>
              <li class="dropdown">
                <a href="#" data-target="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.email %>
                <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Log out", destroy_user_session_path %></li>
                </ul>
              </li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
      <!-- .top nav -->
      <div class="container">
        <div class="panel panel-info">
          <div class="panel-heading">Debugger Panel</div>
          <div class="panel-body">
            <%= params %>
          </div>
        </div>
      </div>
      
      <%= yield %>
      <script>$.material.init()</script>
    </body>
  </html>