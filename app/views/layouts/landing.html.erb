<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ExpiredBoxes-Landing</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>

  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Cookie">
  <%= csrf_meta_tags %>
</head>
<body>

  <nav class="navbar navbar-default navbar-static-top">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand navbar-link" href="/">Expired Box <span class="badge">Beta</span></a>
        <button class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navcol-1">
          <span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span>
        </button>
      </div>
      <div class="collapse navbar-collapse" id="navcol-1">
        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
          <!-- for sm onward -->
          <li class="dropdown hidden-xs">
            <a data-toggle="dropdown" aria-expanded="false" href="#" class="dropdown-toggle"><%= current_user.email %>
              <span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">
                <li><a href="#">Manage Boxes</a></li>
                <li class="divider"></li>
                <li><a href="#">Sign Out</a></li>
              </ul>
            </li>
            <!-- for xs only -->
            <li class="visible-xs-block" role="presentation">
              <a href="<%= edit_user_registration_path %>"><%= current_user.email %></a>
            </li>
            <li class="visible-xs-block" role="presentation">
              <a href="#">Manage Boxes</a>
            </li>
            <li class="divider visible-xs-block"></li>
            <li class="visible-xs-block" role="presentation">
              <%= link_to 'Sign out <span class="glyphicon glyphicon-log-out"></span>'.html_safe, destroy_user_session_path, method: :delete %>
            </li>
            <% else %>
            <li <%= 'class=active' if params[:controller] == 'devise/registrations' && params[:action] == 'new' %> role="presentation">
              <a href="<%= new_user_registration_path %>"> Register <span class="glyphicon glyphicon-user"></span></a>
            </li>
            <li <%= 'class=active' if params[:controller] == 'devise/sessions' && params[:action] == 'new' %> role="presentation">
              <a href="<%= new_user_session_path %>"> Login <span class="glyphicon glyphicon-log-in"></span></a>
            </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>

    <section class="footer">
      <div class="container-fluid">
        <div class="row">
          <div class="col-md-12">
            <p class="text-center">Create with ❤ and hope to reduce the impacts on earth.</p>
          </div>
        </div>
      </div>
    </section>

  </body>
  </html>
