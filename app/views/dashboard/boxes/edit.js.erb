$('.modal').html('<%= escape_javascript(render "dashboard/boxes/forms/modal_edit") %>');

$('.btn-modal').on('click', function() {  
  $(this).attr('disabled', 'disabled');
  $(this).parents('form').submit()
});
